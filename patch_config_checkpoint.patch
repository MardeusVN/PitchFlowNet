*** Begin Patch
*** Update File: ../BanhmiTTS/src/python/banhmi_tts/training/config.py
@@
     kl_weight: float = 1.0
+    kl_warmup_steps: int = 20_000
@@
         ) < 0.0:
             raise ValueError("training loss weights cannot be negative")
+        if self.kl_warmup_steps < 0:
+            raise ValueError("kl_warmup_steps cannot be negative")
@@
     def prosody_teacher_forcing_ratio(self, global_step: int) -> float:
@@
         return self.prosody_teacher_forcing_start + progress * (
             self.prosody_teacher_forcing_end
             - self.prosody_teacher_forcing_start
         )
+
+    def kl_weight_at(self, global_step: int) -> float:
+        """Linearly warm KL from zero to ``kl_weight``."""
+        if global_step < 0:
+            raise ValueError("global_step cannot be negative")
+        if self.kl_warmup_steps == 0:
+            return self.kl_weight
+        progress = min(global_step / self.kl_warmup_steps, 1.0)
+        return self.kl_weight * progress
*** Update File: ../BanhmiTTS/src/python/banhmi_tts/training/checkpoint.py
@@
     state = {
+        "checkpoint_schema_version": 2,
         "global_step": int(global_step),
@@
-    return {
-        "global_step": int(state["global_step"]),
+    schema_version = int(state.get("checkpoint_schema_version", 1))
+    completed_steps = int(state["global_step"])
+    if schema_version == 1:
+        # Legacy checkpoints stored the zero-based index of the last step.
+        completed_steps += 1
+    return {
+        "global_step": completed_steps,
         "extra": state.get("extra", {}),
     }
*** Update File: ../BanhmiTTS/src/python/banhmi_tts/training/__init__.py
@@
-from .trainer import BanhmiTrainer, TrainingStepMetrics
+from .trainer import BanhmiTrainer, TrainingStepMetrics, ValidationStepResult
@@
     "TrainingStepMetrics",
+    "ValidationStepResult",
*** End Patch
