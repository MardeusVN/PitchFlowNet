*** Begin Patch
*** Update File: ../BanhmiTTS/src/python/tests/test_banhmi_training.py
@@
     def test_teacher_forcing_schedule(self):
@@
         self.assertEqual(optimization.prosody_teacher_forcing_ratio(100), 0.0)
+
+    def test_kl_warmup_schedule(self):
+        optimization = OptimizationConfig(kl_weight=1.0, kl_warmup_steps=100)
+        self.assertEqual(optimization.kl_weight_at(0), 0.0)
+        self.assertEqual(optimization.kl_weight_at(50), 0.5)
+        self.assertEqual(optimization.kl_weight_at(100), 1.0)
+        self.assertEqual(optimization.kl_weight_at(200), 1.0)
+
+    def test_validation_is_deterministic_with_fixed_seed(self):
+        trainer = BanhmiTrainer(small_config(), torch.device("cpu"))
+        first = trainer.validate_step(
+            small_batch(),
+            global_step=10,
+            random_generator=torch.Generator().manual_seed(9),
+        )
+        second = trainer.validate_step(
+            small_batch(),
+            global_step=10,
+            random_generator=torch.Generator().manual_seed(9),
+        )
+        self.assertEqual(first.metrics, second.metrics)
+        self.assertTrue(torch.equal(first.generated_audio, second.generated_audio))
+        self.assertTrue(torch.equal(first.target_audio, second.target_audio))
*** End Patch
