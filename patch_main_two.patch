*** Begin Patch
*** Update File: ../BanhmiTTS/src/python/banhmi_tts/training/__main__.py
@@
         metrics = trainer.train_step(batch, global_step)
-        if global_step % 10 == 0:
-            print(json.dumps(asdict(metrics)))
-        if args.checkpoint_every > 0 and (global_step + 1) % args.checkpoint_every == 0:
+        global_step += 1
+        if global_step == 1 or global_step % 10 == 0:
+            print(json.dumps(asdict(metrics)))
+        if args.checkpoint_every > 0 and global_step % args.checkpoint_every == 0:
             save_checkpoint(
-                output_dir / f"checkpoint-{global_step + 1:08d}.pt",
+                output_dir / f"checkpoint-{global_step:08d}.pt",
                 trainer,
                 global_step,
             )
-        global_step += 1
-    save_checkpoint(output_dir / "checkpoint-final.pt", trainer, global_step - 1)
+        if args.validation_every > 0 and global_step % args.validation_every == 0:
+            validation_rows = []
+            first_result = None
+            for batch_index, validation_batch in enumerate(validation_loader):
+                if batch_index >= args.validation_batches:
+                    break
+                result = trainer.validate_step(
+                    validation_batch,
+                    global_step,
+                    random_generator=_device_generator(device, 10_000 + batch_index),
+                )
+                if first_result is None:
+                    first_result = result
+                row = asdict(result.metrics)
+                row.pop("global_step")
+                validation_rows.append(row)
+            summary = _mean_metrics(validation_rows)
+            print(json.dumps({"validation_step": global_step, **summary}))
+            if (
+                first_result is not None
+                and args.sample_every > 0
+                and global_step % args.sample_every == 0
+            ):
+                sample_dir = output_dir / "validation_samples"
+                _write_wav(
+                    sample_dir / f"step-{global_step:08d}-generated.wav",
+                    first_result.generated_audio[0],
+                    sample_rate,
+                )
+                _write_wav(
+                    sample_dir / f"step-{global_step:08d}-target.wav",
+                    first_result.target_audio[0],
+                    sample_rate,
+                )
+    save_checkpoint(output_dir / "checkpoint-final.pt", trainer, global_step)
*** End Patch
